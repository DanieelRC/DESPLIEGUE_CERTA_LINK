<main class="mt-5">
    <div class="container d-flex justify-content-center">
        <!-- Usamos .card y .shadow-lg para aprovechar los estilos del global.css -->
        <div class="card shadow-lg w-50">
            <!-- Reemplazamos bg-primary text-white text-center por una clase nueva -->
            <div class="card-header align-items-center text-center">
                <h3 class="text-uppercase m-0">CERTA LINK</h3>
            </div>
            <div class="card-body text-center">
                <div class="d-flex flex-column align-items-center gap-3">
                    <form method="post" action="crearUsuario" class="w-100">
                        <!-- Reemplazamos btn-secondary por btn-crear -->
                        <button type="submit" class="btn btn-crear w-100">
                            Crear Usuario
                        </button>
                    </form>
                    <!-- Reemplazamos btn-primary por btn-consultas (o la que prefieras) -->
                    <a href="/iniciarSesion" class="btn btn-consultas w-100">
                        Iniciar Sesión
                    </a>
                </div>
            </div>
        </div>
    </div>
</main>